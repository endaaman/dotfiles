#!/bin/bash

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
  wl-copy "$@"
else
  # xselは標準入力を期待するので、引数を標準入力に渡す
  if [ $# -gt 0 ]; then
    printf "%s" "$@" | xsel --clipboard --input
  else
    xsel --clipboard --input
  fi
fi
