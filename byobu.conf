# vi: set ft=tmux :

set-option -g renumber-windows on

bind-key -n M-h prev
bind-key -n M-l next
bind-key -n M-p prev
bind-key -n M-n next

bind-key -n M-t new-window -c "#{pane_current_path}"

bind-key -n M-v copy-mode \; display "Copy mode"

bind-key -n M-S-Left swap-window -t -1
bind-key -n M-S-Right swap-window -t +1

# bind-key -n F5 source  source-file ~/.tmux.conf \; display-message "Reloaded"

bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection
bind-key -t vi-copy Enter copy-pipe "cat | xsel -i --clipboard"
bind-key -t vi-copy y copy-pipe "cat | xsel -i --clipboard"
